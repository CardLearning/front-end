name: CI

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: Lint and package static site
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build static export
        run: npm run build:static

      - name: Package static artifact
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p artifacts
          cd out
          zip -r ../artifacts/static-site.zip .

      - name: Upload static artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: static-site
          path: artifacts/static-site.zip
          if-no-files-found: error
          retention-days: 14
